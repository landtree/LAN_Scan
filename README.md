# LAN_Scan
 A QT based photogrammetry scanner. It uses meshroom as its photogrammetry engine, an RPICam to take photos on plater, sends them via WIFI to the QT server, then starts the reconstruction. 
